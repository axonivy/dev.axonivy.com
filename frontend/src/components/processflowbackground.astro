---
const tasks = [
  {
    x: 311,
    y: 153,
    w: 118,
    h: 50,
    lines: ["Enter", "Request"],
    icon: "user",
  },
  {
    x: 311,
    y: 333,
    w: 118,
    h: 50,
    lines: ["Accept", "Roles"],
    icon: "user",
  },
  {
    x: 558,
    y: 267,
    w: 124,
    h: 46,
    lines: ["Decrement", "Counter"],
    icon: "code",
  },
  {
    x: 684,
    y: 495,
    w: 112,
    h: 50,
    lines: ["Verify", "Request"],
    icon: "user",
  },
];

// Maps the 900x640 canvas area onto the real screenshot's canvas area.
const DIAGRAM_TRANSFORM = "translate(70, -4.1) scale(3.4511)";
---

<div
  class="process-flow-bg pointer-events-none flex h-full w-full items-center overflow-hidden"
>
  <div class="mr-[-2%] ml-[50%] flex h-full w-1/2 items-center justify-end">
    <div
      class="relative aspect-4032/2324 h-auto shrink-0 overflow-visible"
      style="width: 130%; transform: translateX(25%);"
    >
      <!-- ============ SCREENSHOT ============ -->
      <img
        src="/src/assets/vscode-process-frame.png"
        alt="Axon Ivy PRO Designer window"
        class="absolute inset-0 h-full w-full object-contain"
        draggable="false"
      />

      <!-- ============ ANIMATED DIAGRAM ============ -->
      <svg
        viewBox="0 0 4032 2324"
        class="absolute inset-0 h-full w-full overflow-visible"
        role="img"
        aria-label="Axon Ivy process designer showing an onboarding process diagram"
      >
        <defs>
          <marker
            id="arrow"
            viewBox="0 0 10 10"
            refX="9"
            refY="5"
            markerWidth="6.5"
            markerHeight="6.5"
            orient="auto-start-reverse"
          >
            <path d="M0,0 L10,5 L0,10 z" fill="var(--n900)"></path>
          </marker>
          <filter id="ballGlow" x="-100%" y="-100%" width="300%" height="300%">
            <feGaussianBlur stdDeviation="4" result="blur"></feGaussianBlur>
            <feMerge>
              <feMergeNode in="blur"></feMergeNode>
              <feMergeNode in="SourceGraphic"></feMergeNode>
            </feMerge>
          </filter>
          <pattern
            id="dotGrid"
            width="16"
            height="16"
            patternUnits="userSpaceOnUse"
          >
            <rect width="16" height="16" fill="var(--background)"></rect>
            <circle cx="1.2" cy="1.2" r="0.75" fill="var(--n300, #d1d5db)"
            ></circle>
          </pattern>
        </defs>

        <g transform={DIAGRAM_TRANSFORM}>
          <rect x="50" y="82" width="850" height="532" fill="url(#dotGrid)"
          ></rect>
          <path
            d="M128,110 L108,110 Q100,110 100,118 L100,572 Q100,580 108,580 L128,580 Z"
            fill="var(--n25)"></path>
          <path
            d="M900,110 L108,110 Q100,110 100,118 L100,572 Q100,580 108,580 L900,580"
            fill="none"
            stroke="var(--n400)"
            stroke-width="0.75"></path>
          <line
            x1="128"
            y1="110"
            x2="128"
            y2="580"
            stroke="var(--n400)"
            stroke-width="0.75"></line>
          <line
            x1="128"
            y1="250"
            x2="900"
            y2="250"
            stroke="var(--n400)"
            stroke-width="0.75"></line>
          <line
            x1="128"
            y1="460"
            x2="900"
            y2="460"
            stroke="var(--n400)"
            stroke-width="0.75"></line>

          <text
            x="117"
            y="355"
            text-anchor="middle"
            font-family="ui-sans-serif, system-ui"
            font-size="10.5"
            fill="var(--foreground)"
            transform="rotate(-90 117 355)">Department</text
          >
          <text
            x="148"
            y="180"
            text-anchor="middle"
            font-family="ui-sans-serif, system-ui"
            font-size="10.5"
            fill="var(--foreground)"
            transform="rotate(-90 148 180)">Employee</text
          >
          <text
            x="148"
            y="355"
            text-anchor="middle"
            font-family="ui-sans-serif, system-ui"
            font-size="10.5"
            fill="var(--foreground)"
            transform="rotate(-90 148 355)">Dynamic Tasks</text
          >
          <text
            x="148"
            y="520"
            text-anchor="middle"
            font-family="ui-sans-serif, system-ui"
            font-size="10.5"
            fill="var(--foreground)"
            transform="rotate(-90 148 520)">Manager</text
          >

          <!-- connectors -->
          <g fill="none" stroke="var(--n900)" stroke-width="1">
            <path d="M243,178 L311,178" marker-end="url(#arrow)"></path>
            <path d="M370,203 L370,333" marker-end="url(#arrow)"></path>
            <path d="M429,358 L485,358" marker-end="url(#arrow)"></path>
            <path d="M500,373 L500,410" marker-end="url(#arrow)"></path>
            <path d="M515,358 L725,358" marker-end="url(#arrow)"></path>
            <path
              d="M740,343 L740,298 Q740,290 732,290 L682,290"
              marker-end="url(#arrow)"></path>
            <path
              d="M558,290 L508,290 Q500,290 500,298 L500,343"
              marker-end="url(#arrow)"></path>
            <path d="M740,373 L740,495" marker-end="url(#arrow)"></path>
            <path d="M796,520 L890,520" marker-end="url(#arrow)"></path>
            <path d="M780,614 L780,579"></path>
          </g>

          <!-- start event -->
          <circle
            data-node="start"
            cx="230"
            cy="178"
            r="13"
            fill="var(--background)"
            stroke="var(--foreground)"
            stroke-width="1"></circle>
          <text
            x="230"
            y="201"
            text-anchor="middle"
            font-family="ui-sans-serif, system-ui"
            font-size="10"
            fill="var(--n700)">Start</text
          >

          <!-- task boxes: content (icon + text). -->
          {
            tasks.map((t, i) => {
              const cy = t.y + t.h / 2;
              const iconX = t.x + 14;
              const iconY = cy - 12;
              const textX = iconX + 24 + 10;
              return (
                <>
                  <rect
                    data-node={`task-${i}`}
                    x={t.x}
                    y={t.y}
                    width={t.w}
                    height={t.h}
                    rx="5"
                    fill="var(--n25)"
                    stroke="var(--foreground)"
                    stroke-width="1"
                  />
                  <rect
                    x={iconX}
                    y={iconY}
                    width="24"
                    height="24"
                    rx="4"
                    fill="var(--accent)"
                  />
                  {t.icon === "code" ? (
                    <g
                      stroke="var(--primary)"
                      stroke-width="1.3"
                      fill="none"
                      stroke-linecap="round"
                      stroke-linejoin="round"
                    >
                      {/* "<" */}
                      <path d={`M${iconX + 8},${iconY + 7} l-4,5 l4,5`} />
                      {/* ">" */}
                      <path d={`M${iconX + 16},${iconY + 7} l4,5 l-4,5`} />
                      {/* "/" */}
                      <line
                        x1={iconX + 13}
                        y1={iconY + 5}
                        x2={iconX + 11}
                        y2={iconY + 19}
                      />
                    </g>
                  ) : (
                    <g stroke="var(--primary)" stroke-linecap="round">
                      <line
                        x1={iconX + 3}
                        y1={iconY + 8}
                        x2={iconX + 9}
                        y2={iconY + 8}
                        stroke-width="1"
                      />
                      <line
                        x1={iconX + 3}
                        y1={iconY + 12}
                        x2={iconX + 8}
                        y2={iconY + 12}
                        stroke-width="1"
                      />
                      <line
                        x1={iconX + 3}
                        y1={iconY + 16}
                        x2={iconX + 7}
                        y2={iconY + 16}
                        stroke-width="1"
                      />
                      <circle
                        cx={iconX + 16.5}
                        cy={iconY + 9}
                        r="2.3"
                        fill="none"
                        stroke-width="1.15"
                      />
                      <path
                        d={`M${iconX + 11.8},${iconY + 16} a4.7,4.7 0 0 1 9.4,0`}
                        fill="none"
                        stroke-width="1.15"
                      />
                    </g>
                  )}
                  <text
                    x={textX}
                    y={cy - 4}
                    font-family="ui-sans-serif, system-ui"
                    font-size="11.5"
                    fill="var(--foreground)"
                  >
                    {t.lines[0]}
                  </text>
                  <text
                    x={textX}
                    y={cy + 12}
                    font-family="ui-sans-serif, system-ui"
                    font-size="11.5"
                    fill="var(--foreground)"
                  >
                    {t.lines[1]}
                  </text>
                </>
              );
            })
          }

          <!-- Gateway 1 (exclusive) -->
          <path
            data-node="gateway1"
            d="M497.4,345.6 Q500,343 502.6,345.6 L512.4,355.4 Q515,358 512.4,360.6 L502.6,370.4 Q500,373 497.4,370.4 L487.6,360.6 Q485,358 487.6,355.4 Z"
            fill="var(--background)"
            stroke="var(--foreground)"
            stroke-width="1"></path>
          <g
            data-node="gateway1"
            stroke="var(--foreground)"
            stroke-width="1"
            stroke-linecap="round"
            ><line x1="495.5" y1="353.5" x2="504.5" y2="362.5"></line><line
              x1="504.5"
              y1="353.5"
              x2="495.5"
              y2="362.5"></line></g
          >

          <!-- Finish end event -->
          <circle
            data-node="finish"
            cx="500"
            cy="422"
            r="12"
            fill="var(--background)"
            stroke="var(--foreground)"
            stroke-width="2"></circle>

          <!-- Check gateway (circle-plus) -->
          <path
            data-node="checkgateway"
            d="M737.4,345.6 Q740,343 742.6,345.6 L752.4,355.4 Q755,358 752.4,360.6 L742.6,370.4 Q740,373 737.4,370.4 L727.6,360.6 Q725,358 727.6,355.4 Z"
            fill="var(--background)"
            stroke="var(--foreground)"
            stroke-width="1"></path>
          <circle
            data-node="checkgateway"
            cx="740"
            cy="358"
            r="7"
            fill="var(--background)"
            stroke="var(--foreground)"
            stroke-width="1"></circle>
          <g
            data-node="checkgateway"
            stroke="var(--foreground)"
            stroke-width="1"
          >
            <line x1="740" y1="354" x2="740" y2="362"></line>
            <line x1="736" y1="358" x2="744" y2="358"></line>
          </g>

          <!-- boundary escalation event on Verify Request -->
          <circle
            cx="780"
            cy="549"
            r="13"
            fill="var(--background)"
            stroke="var(--foreground)"
            stroke-width="1"></circle>
          <circle
            cx="780"
            cy="549"
            r="11"
            fill="var(--background)"
            stroke="var(--foreground)"
            stroke-width="1"></circle>
          <path
            d="M780,544 L785,553 L775,553 Z"
            fill="none"
            stroke="var(--foreground)"
            stroke-width="1"
            stroke-linejoin="round"></path>
          <text
            x="780"
            y="574"
            text-anchor="middle"
            font-family="ui-sans-serif, system-ui"
            font-size="10"
            fill="var(--foreground)">dynamic:cancel</text
          >

          <!-- ============ animated token: cycles through all three real routes ============ -->
          <path
            id="routeFinish"
            d="M230,178 L311,178 L370,178 L370,203 L370,333 L370,358 L429,358 L485,358 L500,358 L500,373 L500,422"
            fill="none"
            stroke="none"></path>
          <path
            id="routeLoop"
            d="M230,178 L311,178 L370,178 L370,203 L370,333 L370,358 L429,358 L485,358 L500,358 L515,358 L725,358 L740,358 L740,343 L740,290 L682,290 L670,290 L558,290 L508,290 Q500,290 500,298 L500,343 L500,358 L515,358 L725,358 L740,358 L740,373 L740,495 L740,520 L796,520 L890,520"
            fill="none"
            stroke="none"></path>
          <path
            id="routeDirect"
            d="M230,178 L311,178 L370,178 L370,203 L370,333 L370,358 L429,358 L485,358 L500,358 L515,358 L725,358 L740,358 L740,373 L740,495 L740,520 L796,520 L890,520"
            fill="none"
            stroke="none"></path>

          <circle
            id="flow-token"
            r="5"
            fill="var(--success)"
            filter="url(#ballGlow)"
          >
            <animateMotion
              id="animFinish"
              begin="0s;animDirect.end"
              dur="4.2s"
              fill="freeze"
              repeatCount="1"
            >
              <mpath href="#routeFinish"></mpath>
            </animateMotion>
            <animateMotion
              id="animLoop"
              begin="animFinish.end"
              dur="14s"
              fill="freeze"
              repeatCount="1"
            >
              <mpath href="#routeLoop"></mpath>
            </animateMotion>
            <animateMotion
              id="animDirect"
              begin="animLoop.end"
              dur="10s"
              fill="freeze"
              repeatCount="1"
            >
              <mpath href="#routeDirect"></mpath>
            </animateMotion>
          </circle>

          <!-- "visited" badges are injected here at runtime by the script -->
          <g id="badge-layer"></g>
        </g>
      </svg>
    </div>
  </div>
</div>

<style>
  .process-flow-bg [data-node] {
    transition: stroke 0.25s ease;
  }
  .process-flow-bg .node-visited {
    stroke: var(--success) !important;
  }
</style>

<script>
  // Highlights each shape as the token passes it and shows a "1" badge at
  // its top-right corner; clears when the token hands off to the next
  // route. Route identity comes from SMIL's beginEvent.
  // Progress within a route is elapsed-time-since-beginEvent, so it
  // self-corrects every route change instead of drifting.

  type RouteName = "routeFinish" | "routeLoop" | "routeDirect";
  type Vertex = [number, number];
  type Waypoint = [number, string];

  function initProcessFlowBg(root: Element): void {
    const svg = root.querySelector("svg");
    if (!svg) return;

    const VERTS: Record<RouteName, Vertex[]> = {
      routeFinish: [
        [230, 178],
        [311, 178],
        [370, 178],
        [370, 203],
        [370, 333],
        [370, 358],
        [429, 358],
        [485, 358],
        [500, 358],
        [500, 373],
        [500, 422],
      ],
      routeLoop: [
        [230, 178],
        [311, 178],
        [370, 178],
        [370, 203],
        [370, 333],
        [370, 358],
        [429, 358],
        [485, 358],
        [500, 358],
        [515, 358],
        [725, 358],
        [740, 358],
        [740, 343],
        [740, 290],
        [682, 290],
        [670, 290],
        [558, 290],
        [508, 290],
        [500, 298],
        [500, 343],
        [500, 358],
        [515, 358],
        [725, 358],
        [740, 358],
        [740, 373],
        [740, 495],
        [740, 520],
        [796, 520],
        [890, 520],
      ],
      routeDirect: [
        [230, 178],
        [311, 178],
        [370, 178],
        [370, 203],
        [370, 333],
        [370, 358],
        [429, 358],
        [485, 358],
        [500, 358],
        [515, 358],
        [725, 358],
        [740, 358],
        [740, 373],
        [740, 495],
        [740, 520],
        [796, 520],
        [890, 520],
      ],
    };

    // [vertex index, node name] — a name can repeat within a route (e.g.
    // Gateway 1 and the check gateway are each visited twice on the loop).
    const WAYPOINTS: Record<RouteName, Waypoint[]> = {
      routeFinish: [
        [0, "start"],
        [2, "task-0"],
        [5, "task-1"],
        [8, "gateway1"],
        [10, "finish"],
      ],
      routeLoop: [
        [0, "start"],
        [2, "task-0"],
        [5, "task-1"],
        [8, "gateway1"],
        [11, "checkgateway"],
        [15, "task-2"],
        [20, "gateway1"],
        [23, "checkgateway"],
        [26, "task-3"],
      ],
      routeDirect: [
        [0, "start"],
        [2, "task-0"],
        [5, "task-1"],
        [8, "gateway1"],
        [11, "checkgateway"],
        [14, "task-3"],
      ],
    };

    const ORDER: RouteName[] = ["routeFinish", "routeLoop", "routeDirect"];

    const badgeLayer = svg.querySelector("#badge-layer");
    const nodeNames: string[] = Array.from(
      new Set(
        Object.values(WAYPOINTS)
          .flat()
          .map(([, name]) => name),
      ),
    );
    const badgeEls = new Map<string, SVGGElement>();
    const BADGE_W = 20;
    const BADGE_H = 13;

    if (badgeLayer) {
      for (const name of nodeNames) {
        const shape = svg.querySelector(
          `[data-node="${name}"]`,
        ) as SVGGraphicsElement | null;
        if (!shape || typeof shape.getBBox !== "function") continue;
        const bbox = shape.getBBox();
        const x = bbox.x + bbox.width;
        const y = bbox.y;

        const g = document.createElementNS(
          "http://www.w3.org/2000/svg",
          "g",
        ) as SVGGElement;
        g.setAttribute("data-badge", name);
        g.setAttribute("transform", `translate(${x}, ${y})`);
        // Astro's scoped <style> won't match elements created at runtime,
        // so styling here is inline rather than via CSS classes.
        g.style.opacity = "0";
        g.style.transition = "opacity 0.2s ease";

        const rect = document.createElementNS(
          "http://www.w3.org/2000/svg",
          "rect",
        ) as SVGRectElement;
        rect.setAttribute("x", String(-BADGE_W / 2));
        rect.setAttribute("y", String(-BADGE_H / 2));
        rect.setAttribute("width", String(BADGE_W));
        rect.setAttribute("height", String(BADGE_H));
        rect.setAttribute("rx", String(BADGE_H / 2));
        rect.setAttribute("ry", String(BADGE_H / 2));
        rect.setAttribute("fill", "var(--background)");
        rect.setAttribute("stroke", "var(--success)");
        rect.setAttribute("stroke-width", "1");

        const text = document.createElementNS(
          "http://www.w3.org/2000/svg",
          "text",
        ) as SVGTextElement;
        text.setAttribute("y", "0.5");
        text.setAttribute("fill", "var(--success)");
        text.setAttribute("font-family", "ui-sans-serif, system-ui");
        text.setAttribute("font-size", "9");
        text.setAttribute("font-weight", "700");
        text.setAttribute("text-anchor", "middle");
        text.setAttribute("dominant-baseline", "central");
        text.textContent = "1";

        g.append(rect, text);
        badgeLayer.append(g);
        badgeEls.set(name, g);
      }
    }

    function highlight(name: string): void {
      svg!
        .querySelectorAll(`[data-node="${name}"]`)
        .forEach((el) => el.classList.add("node-visited"));
      const badge = badgeEls.get(name);
      if (badge) badge.style.opacity = "1";
    }

    function unhighlight(name: string): void {
      svg!
        .querySelectorAll(`[data-node="${name}"]`)
        .forEach((el) => el.classList.remove("node-visited"));
      const badge = badgeEls.get(name);
      if (badge) badge.style.opacity = "0";
    }

    const routeAnimEls: Record<RouteName, Element | null> = {
      routeFinish: svg.querySelector("#animFinish"),
      routeLoop: svg.querySelector("#animLoop"),
      routeDirect: svg.querySelector("#animDirect"),
    };
    const DURATIONS: Record<RouteName, number> = {
      routeFinish: 4.2,
      routeLoop: 14,
      routeDirect: 10,
    };

    // Distance-fraction-based waypoint timing (animateMotion defaults to
    // constant-velocity "paced" motion, so time is proportional to
    // distance along the path).
    const routeWaypointTimes = {} as Record<
      RouteName,
      { name: string; time: number }[]
    >;
    for (const routeName of ORDER) {
      const verts = VERTS[routeName];
      const dist: number[] = [0];
      for (let i = 1; i < verts.length; i++) {
        const [x1, y1] = verts[i - 1];
        const [x2, y2] = verts[i];
        dist.push(dist[i - 1] + Math.hypot(x2 - x1, y2 - y1));
      }
      const total = dist[dist.length - 1];
      const dur = DURATIONS[routeName];
      routeWaypointTimes[routeName] = WAYPOINTS[routeName].map(
        ([idx, name]) => ({
          name,
          // Pull anything within the final 50ms back, so a waypoint at the
          // route's exact end (e.g. "finish") can't race the next route's
          // beginEvent reset.
          time: Math.min((dist[idx] / total) * dur, dur - 0.05),
        }),
      );
    }

    let currentRoute: RouteName = "routeFinish";
    let routeStartTime = performance.now();
    let previousActive = new Set<string>();

    for (const routeName of ORDER) {
      const animEl = routeAnimEls[routeName];
      if (!animEl) continue;
      animEl.addEventListener("beginEvent", () => {
        for (const name of nodeNames) unhighlight(name);
        previousActive = new Set();
        currentRoute = routeName;
        routeStartTime = performance.now();
      });
    }

    function tick(): void {
      const elapsed = (performance.now() - routeStartTime) / 1000;
      const dur = DURATIONS[currentRoute];
      const t = Math.min(Math.max(elapsed, 0), dur);

      const active = new Set<string>();
      for (const wp of routeWaypointTimes[currentRoute]) {
        if (wp.time <= t) active.add(wp.name);
      }

      for (const name of nodeNames) {
        const isActive = active.has(name);
        const wasActive = previousActive.has(name);
        if (isActive && !wasActive) highlight(name);
        else if (!isActive && wasActive) unhighlight(name);
      }
      previousActive = active;

      requestAnimationFrame(tick);
    }
    requestAnimationFrame(tick);
  }

  document
    .querySelectorAll(".process-flow-bg")
    .forEach((root) => initProcessFlowBg(root));
</script>
